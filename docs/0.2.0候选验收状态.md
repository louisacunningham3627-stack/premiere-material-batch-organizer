# 0.2.0 候选验收状态

## 0.2.10 静默整理候选，已安装待真机验收

2026-09-09：用户正常关闭 Premiere 后完成 Windows 用户级安装。安装版 `manifest.json` 为 `0.2.10`，本地助手检查返回 `status=available`、`mediaTouched=false`；随后启动 Premiere 2026（PID `57520`，窗口标题“Adobe Premiere”，响应正常）。未打开旧工程、未触碰两个大视频。由于 Computer Use 接口返回 `unsupported Codex auth method: apikey`，本轮未自动点击 UXP 菜单；仍需在 Premiere 中打开【窗口 > UXP 插件 > 赫朝素材自动整理】完成独立小素材真机验收。

2026-09-07：用户报告自动刷新闪烁、多次执行/保存打断预览，以及下载目录出现多份回收暂存空目录。本轮在既有 E 项目修复，未修改 PR 自带自动保存设置。

已确认的代码触发链：2.6 秒轮询强制刷新上下文，再切换 busy 界面并重建列表；DIRTY 事件和导入事件同时催促新一轮扫描；每个素材与每次回收重试分别保存；成功改链后无条件 refreshMedia；原生回收成功后没有收掉暂存空目录。这些代码能解释重复工作，但尚未以真实 PR 录屏/性能追踪量化每一种顿挫的占比。

候选变更：

- 自动检查不显示整页过渡态，相同可见状态不重新渲染；空闲轮询改十秒，待写入素材保留较快检查。连续导入 1.2 秒合并，请求单飞；DIRTY 不触发连锁扫描。
- 本轮素材先逐项备妥并持久暂缓，全部原件保留，再统一保存后逐项回收。测试中的 12 个新增素材仅一次保存。保存失败停本轮，不对每一项重复保存。
- 已在线且路径正确时不强制 refreshMedia。仅在本次面板会话中，保存前后完整链接快照一致且磁盘工程身份未变时复用保存证据；重开面板、工程磁盘变化、链接不匹配均不能复用。回收前仍逐项核对身份和完整引用。
- 读取时间线及源监视器的播放头位置，避让正在移动的预览；复制中途开始预览时可保留目标，延后改链/保存。只用官方 25.6 起公开的读取接口，不发播放、暂停或跳转。采样失败仅失去预览避让，不让整理永久停住。官方 Project.save 无无界面参数，不保证零顿挫或消除采样后的极窄竞态。
- 原生回收取得成功凭据后，按已持有的本次目录句柄请求删除空目录；非空或无目录删除权限时保持，绝不递归。真实回收凭据查询/中断恢复不受空壳移除影响。

已验证：`npm run check` 407 项，386 通过、21 明确跳过、0 失败；构建校验通过。10/10 Windows 原生独立小文件测试通过，含空壳消失、额外文件保留、锁冲突、取消、恢复及回执丢失后查询。新增覆盖 12 项一次保存、30 次导入一次检查、无变化不重绘、占用重试不重存、磁盘变化重新保存、保存失败原件全保留、预览移动不扫描/保存及复制中途延后改链。三尺寸浏览器界面检查通过，截图 `work/ui-background-0.2.10/`；这不是 UXP 实机无顿挫证明。

截图中的 Downloads 下 8 个 `.premiere-material-recycle-<id>` 均为普通空目录，逐一与相同安装 token、请求 ID、暂存路径及 recycled/唯一凭据核对一致后，以非递归空目录删除清理，复核全不存在；没有删除素材或清空回收站，`.safetensors` 未动。8 个 ID 为 267e244c505513a29fcafbd329c4bbcc、3c45d3538cb1ac454f2459ffb8cfc0f6、5324610985c380aedfe00012589a9a05、5fbfd895fc9026c55bbae5f1c4cd1ec7、74e390551d7155018bc9f9c4316e1b4f、a9ab758049459ce42d52f79bbac1f5bd、af0ed00075d45f4861d60f767e8f90d5、cb83e26ee6d6fa6e713b4cb8eb34e4f9。回收原件仍可从系统回收站找回。

安装门禁：仍安装 0.2.9。正常退出请求时发现用户已切到 `K:\2026-9-5 MC瓦罗兰特\MC瓦罗兰特 待粗剪.prproj *`，PR 未退出，故未强杀、未热替换、未操作其未保存内容。用户正常保存关闭后才能安装 0.2.10 并做真实批量导入/预览验收。未使用用户大视频测试，未提交推送。macOS 包已同步构建，SHA-256 `e7c5363ba331f521318f410d011d40cb4f853bbc89f9a0c1afabc9ed654de7e7`，不声明废纸篓/宿主通过。

## 0.2.9 已安装基线

2026-09-07 05:56：Premiere 经正常关闭请求退出后安装 0.2.9，19 个发布文件与 dist 逐项 SHA-256 一致；安装额外生成正常的 `native/windows/bridge-location.json` 本机配置。旧 0.2.8 备份尾缀为 `before-20260907-055653-be921a32`。安装后文件服务可用，Premiere 已重新启动并响应正常，未自动打开旧工程。

本版将普通文件占用改为后台等待。原生助手保留 Win32 错误码，只对共享/锁冲突 32、33 在 1.2 秒内短暂重试；仍被占用时，经认证失败终态核对后将事务持久暂缓，让其他素材继续，再按 10、20、40、60 秒退避重试。不会重新复制目标，不要求逐项确认。暂停或关闭面板停止自动重试；只有所属当前工程可接续。权限及普通单项失败保留现场；回收结果未知、提交未决、写锁冲突和状态损坏仍阻断，不放宽身份、工程链接与保存检查。

原 0.2.8 已保存且回收失败的可识别记录可自动接管；旧手工暂缓及身份不完整的历史记录不自动回收。主页只增加一行等待数量/文件名，沿用原界面布局与状态样式，暂时占用不切入恢复页；打开当前文件夹不再排在素材操作队列之后。

验证结果：

- `npm run check`：397 项，377 通过、20 明确跳过、0 失败；构建和安全契约通过。
- 本轮启用的 Windows 原生测试 9/9 通过，包括真实小文件回收、短锁释放、持续占用、等待中取消、暂存中断恢复及不覆盖原路径替换文件。只使用生成的小测试文件。
- 新增主流程覆盖：一项被占用仍整理其他素材、重开不重复复制、退避、暂停、旧版失败接管、未知请求不放行、持久化失败、保存失败、切工程/隐藏面板和目标变化。
- `scripts/check-background-ui.mjs` 在 300x420、540x480、760x850 通过：无横向溢出或重叠，设置可返回，等待时目录按钮可用，暂停/继续状态正确。截图位于 `work/ui-background-0.2.9/`。这是浏览器布局证据，不是 UXP 真机证据。
- 已安装代码执行 `node scripts/check-installed-helper.mjs --exercise` 通过：精确身份、系统回收唯一凭据、重复请求不重复回收、目标内容保持、目录打开回执。独立 49 字节文件已进回收站，可恢复；证据为 `work/installed-helper-smoke/run-bR1Xbl/验收结果.json`。
- macOS 测试包同步至 0.2.9，ZIP SHA-256：`5155ded6110cb291f83feeccd975ac555937297b2e6c94d09eb19452d066398d`。仍不标记系统废纸篓及 Premiere 宿主已支持。

剩余门禁：本版真实 Premiere 导入、补链、保存及自动等待后回收仍需独立小素材验收；未测试用户大视频，也未手工继续真实 GIF 回收或改写遗留事务。没有提交或推送。以下版本章节均为历史快照，不能当作当前安装状态。

## 0.2.7 历史候选

安装核验（2026-09-06 21:42）：已安装0.2.7，dist与安装19/19文件同哈希，旧版备份尾缀 before-20260906-214239-4dc2c32c。后台服务身份、自启动所有权、已运行实例重复Start均通过。完整自动检查378项中361通过、17明确跳过、0失败。已安装代码与原生程序运行 `node scripts/check-installed-helper.mjs --exercise` 通过：精确身份、真实系统回收、相同回执不重复回收、目标内容保持、目录打开回执。证据在 `work/installed-helper-smoke/run-9Nt7Y1/验收结果.json`。Premiere 已重新启动且响应正常；未自动打开旧大视频工程或进行宿主回收验收。

2026-09-06：0.2.6 将 Windows 通信改为安装器启动的本地文件服务，免去 UXP 外部启动确认。0.2.7 随后修复实际安装测试抓到的回执竞态：reveal-result.json 已创建但写句柄未关闭，桥接抢先读取得到 EBUSY。identity/probe/reveal 现在统一完整写入并刷新后排他发布；reveal 独立占位防重复打开。

服务只接受认证会话的五类请求，不监听网络，最多四个子操作，同一操作不并发执行。升级正常排空后再换文件，移出旧版失败会重新启动仍在原位的服务；卸载移动失败也恢复服务及自启动。服务启动可复用身份核对一致的现有实例，不能把重复启动误报为故障。

已验证：7/7 Windows 原生小文件及文件服务测试，包含真实系统回收、唯一凭据、重复查询不重复回收、源占位保护、暂存中断恢复，以及连续24次身份/预检响应。12/12安装测试包含升级/卸载移动失败后服务恢复。已安装0.2.6测试的源文件回收及目标保持通过，打开的正确测试目录也由资源管理器只读树确认；当时的回执抢读错误已在0.2.7修复并在安装后复验通过。

资源管理器只读树进一步确认打开的是本轮run-9Nt7Y1测试目录。macOS 0.2.7包已同步重建（SHA-256：e4bf2bcc894a3246f80ae121c76bf8ebe7a8b4880808704a14e8de25dd90b343），仍无系统废纸篓及宿主支持声明。

真实 Premiere UXP 内导入、自动补链、工程保存、回收完整闭环仍未验收，不能用上述结果代替。用户两份大视频与历史事务未动。以下均为历史快照，不代表当前安装状态。

## 0.2.5 未安装候选

安装更新（2026-09-06 21:16）：确认PR退出后已安装0.2.5，19文件校验通过，备份尾缀 before-20260906-211623-ed6def13；安装后无素材协议预检 available，已重开PR。此条替代以下未安装快照，UXP功能验收仍待完成。

移除已暂缓历史的首页注入，保留事务和扫描排除。hechao-logo.png 后台可读，但普通 JS ino 数值丢精度：30680772461756344，bigint 真值30680772461756346。新增认证、只读原生 identity 响应，在宿主缺精确身份时使用，并检查前后元数据变化。目录按钮增加打开中/已请求状态。

359 自动测试通过，6/6 Windows 原生测试通过；新增原生身份测试验证精确编号和源内容不变。当前0.2.5构建已准备，仍安装0.2.4。PR关闭请求被模态对话框挡住且标题带星号；桌面激活失败，未强杀未丢弃修改。UXP实际身份降级、目录打开和完整回收链未验收，不能报告全部完成。

## 0.2.4 已安装候选

2026-09-06 21:02：真实 UXP 日志说明打开位置的按钮被调用，但 file scheme 被拒绝；之前对“按钮不响应”的事件桥假设不能当作已确认根因。新增认证、短期、排他 reveal 请求，仅允许规范非重解析目录，原生助手通过固定系统 Explorer 打开目录，不执行任意命令或媒体回收。保留 openPath 原生入口，只有宿主不支持的错误才降级，访问拒绝和 I/O 错误不掩盖。

357 通过、15 跳过、构建通过；5/5 原生小文件回收恢复通过。正常请求 PR 退出且确认退出后安装，19 文件校验、旧版备份尾缀 before-20260906-210230-1c4e98cd。Windows 协议 probe available，PR 已重开。用户要求尽量后台处理，未继续鼠标键盘控制。新 reveal 实际 Explorer 打开、UXP 协议调用及完整宿主收尾仍待验收；不得声称彻底修复。未修改大视频或手工清空事务。

## 0.2.3 恢复死路候选

2026-09-06：截图显示当前素材整理验收_1.prproj 与未完成事务所属未命名.prproj 不同。磁盘原工程存在，当前请求仅 request/cancel，无 commit。候选主要按钮改为捕获阶段直接绑定，避免依赖预览 CustomEvent 转发并阻止重复派发；其他工程显示完整所属路径及“找到原工程”入口。暂缓带请求事务前经 bridge.query 核对，仅 cancelled/failed 可将请求移入历史、可靠持久化后暂缓；未知/提交中继续阻止，核对期间上下文变化也阻止。

356 通过、15 跳过、0 失败，构建和打包通过。当前仍装 0.2.2，0.2.3 未安装。桌面只读返回 PR 最小化，工程标题带未保存星号，未激活/强关/操作素材。按钮物理点击、原工程恢复及真实 UXP 启动仍待安装后验收；不能声称用户功能已完成。

## 0.2.2 启动故障候选

2026-09-06 19:33 安装更新：确认 PR 关闭后安装 0.2.2，19 文件经安装器校验，旧版备份尾缀 before-20260906-193309-472d9a00。安装后无素材 Windows 协议预检 available，已启动 PR；未处理真实素材，UXP 内实际启动与回收待验。此条替代下方“未安装”快照。

用户报告 0.2.1 在 PR 内仍不能启动助手。候选移除空扩展名启动权限，目录打开增加 file URI 降级，并在失败提示保留脱敏后的宿主启动原因。尚不能确认权限声明是根因；Windows 协议直接成功不等于 UXP 启动成功。构建及原有 353 项通过，新增诊断脱敏回归。当前安装 0.2.1，PR 运行，0.2.2 未安装、宿主未验收，不得宣称已修复实际故障。

## 0.2.1 本轮续修

安装续验：用户“开始”后确认 Premiere 已退出，安装 0.2.1，19/19 文件与 dist 同哈希。旧版备份为 `C:\Users\Administrator\AppData\Roaming\Adobe\UXP\PluginBackups\com.hechao.premiere.material-batch-organizer-before-20260906-065310-ab989b82`。`node scripts/check-installed-helper.mjs` 经真实已注册 Windows URI 返回 available，不涉及素材或回收提交。已发起 Premiere 启动；UXP 面板内启动与完整宿主验收仍待验证。此条替代下方“仍安装 0.2.0”快照。

2026-09-06：候选增加独立面板确认页，替代宿主可能不可用的 window.confirm；隐藏面板取消确认。增加认证且无素材路径的原生 probe，正常 Windows 归集在建批次目录、建目标及补链前检查助手可用性；失败明确暂停。恢复忙碌时显示当前核验、补链、保存或回收阶段。原生 probe 不执行回收，不代表宿主链路已验收。

最新验证：368 项中 353 通过、15 跳过、0 失败，构建校验通过；另启用 Windows 原生测试 5/5 通过，覆盖有效/无效预检凭据、暂存中断恢复、占位不覆盖、回收前失败、独立小文件真实回收。真实用户素材及两份大视频未触碰。

当前仍安装 0.2.0，Premiere 进程正在运行，未热替换、未强关。0.2.1 尚需正常关闭后安装，真实 UXP 协议启动、确认页及窄面板、隔离双工程补链/保存/回收验收未完成。未提交未推送，不能称全部修复完成。未提交旧请求的自动对账、提交后长等待和安装故障注入仍需后续收口。

更新：2026-09-06。Windows 候选已安装，尚未完成全部宿主验收，未发布到 GitHub。

最新补充：源码/dist 已为 0.2.1，安装仍为 0.2.0。面板增加常驻版本号及构建一致性校验，回收助手启动异常改为明确中文原因。361 项中 347 通过、14 跳过。真实 Sample Media Clip 1.mp4 事务只有 request 和约 3 秒后写入的 cancel，没有助手 lock/ready/commit/result；尚不能区分宿主启动拒绝、用户取消或其他启动错误。旧代码吞掉了原始异常，不能凭记录确定根因。未重试真实素材回收，等待正常关闭 Premiere 后安装并独立小素材复验。

## 当前安装

- 源项目：E:\Codex\Projects\2026-09-02_Premiere素材批次归集插件
- 安装版本：0.2.0。发布文件 18/18 与 dist 的 SHA-256 相同，回收协议命令已登记到本安装助手。
- 旧版备份：C:\Users\Administrator\AppData\Roaming\Adobe\UXP\PluginBackups\com.hechao.premiere.material-batch-organizer-before-20260906-062821-da2c707d
- 独立宿主素材：work/host-acceptance/candidate-xWudJD，三份两秒合成 WAV，每份 192,044 bytes。
- Premiere 已启动。新建流程意外沿用旧 runtime-smoke 测试目录，产生未命名工程并显示同目录旧事务；桌面操作已停止，没有点击大视频恢复或回收。不得把该工程当作新的隔离验收工程。

## 已通过

- npm run check：360 项，346 通过、14 明确跳过、0 失败；构建与源码契约校验通过。
- 其中四项 Windows 原生测试单独启用后全部通过：暂存后中断并恢复、原路径被新文件占用时不覆盖且解除冲突后可重试、回收前失败恢复原件、真实回收及回执丢失后查询同一凭据。
- 两处同尺寸不同内容、核验中变化、取消核验及普通搬运/恢复的源替换、目标替换、断链、离线、上下文变化、回收失败等已有自动回归。
- 缺少 sourceDisposition 的旧永久删除分支已移除。原件只经回收回调；缺少助手时保留原件。unlink 仅清理本次创建且身份匹配的目标/暂存副本。
- 原生助手使用文件句柄排他改名到本次专属暂存目录，限制内容写入并锁住暂存目录，再调用系统回收接口。回收恢复必须使用本次暂存凭据，不退回按旧原路径查历史回收站条目。
- 提交凭据校验回收请求 ID 和搬运事务 ID。成功必须取得唯一系统回收凭据；超时、路径消失或结果未明不算成功。
- 核对协议只恢复原位或查询回收站，不重复执行回收。旧 uncertain 结果须等待新核对回执，冲突解除后的再次查询已实测。
- 日期/手动延迟分批、目录共享设置、保留旧目录、暂缓记录、独立 schema-1 迁移备份已进入候选。
- 桥接缺少卷号/文件标识时在登记前拒绝；协议无响应时取消未提交请求。安装拒绝覆盖外部协议或缺少 URL Protocol 的不完整登记。
- 卸载只移动插件至备份，只注销指向本安装的协议，保留凭据和回收历史。
- 浏览器预览此前验证 300×650 与 540×480 的滚动、保护名单返回、核验进度及取消；这不是 UXP 宿主验收。
- README 已统一为新版规则。macOS 包已重建，不含 Windows 助手，明确废纸篓未支持。ZIP SHA-256：1b7f9400f4d27da676b16fc05c3d7c48ff3d74d2a2e98d34b05af0b6e0b14a71。

## 未通过或仍需补验

1. Premiere 真机完整小素材归集、全部嵌套引用补链与保存、同目录双工程、旧事务恢复、窄面板完整操作和各阶段中断重启。不能用 Node 或本地助手通过替代。
2. 安装协议部分失败回滚、卸载协议所有权及重解析点的完整故障注入回归，当前仅普通隔离安装/卸载与实际候选安装通过。
3. 暂存路径需在异常恢复界面更直接展示。目前助手异常文本包含暂存位置，普通源路径栏仍显示旧原路径；回收站“原位置”为专属暂存目录，原文件名保留。空暂存目录暂时保留。
4. 无法保证对抗拥有同等权限、故意篡改父目录、凭据或暂存目录的恶意程序；不要让同步/清理工具干预这些位置。
5. 当前源码未提交/推送。完成宿主验收前不标记正式稳定版。
6. Windows 桌面点击触发回收前，按 computer-use 技能需要用户即时确认；目前未进行该步骤。

## 受保护现场

- C:\Users\Administrator\Downloads\318完整版.mp4
- work/runtime-smoke-20260903-194814/测试工程/素材/001_初始素材/318完整版.mp4

本轮核对两处各为 5,802,589,846 bytes，未搬运、回收或作为测试材料。仅在独立小素材闭环通过后，由用户点击“核验并继续”。
